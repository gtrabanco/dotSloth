# review-findings — fix/329-bun-dump-readonly-error

Auto-generated by /review-change. Each fix-now finding gets a row; `folded: no` until /fold-findings processes it.

| ID | file:line | axis | severity | class | route | folded |
|----|-----------|------|----------|-------|-------|--------|
| F1 | scripts/package/dump | format | med | fix-now | fold into phase | yes |
| F2 | scripts/package/dump:29-33 | code | low | fix-now | fold into phase | yes |
| F3 | scripts/package/dump:59 | code | low | fix-now | fold into phase | false-positive |

## Fold history

### Fold run 1 (2026-07-23)

- **F1** — **FIXED** by running `shfmt -w scripts/package/dump`. All 2-space indentation converted to tabs (commit `8f1c9f2`). `shfmt --diff` now returns clean.
- **F2** — **NO-OP** — npm/volta compound condition was already correct (bash `[[ ]] && [[ ]]` precedence is well-defined). No change applied.
- **F3** — **FALSE POSITIVE** — no second `command_exists "$package_manager" "dump"` call exists in the file. `grep -n 'command_exists' scripts/package/dump` confirms single occurrence at line 47.

### Fold run 2 (2026-07-23)

- **F1** — **RE-FIXED** — previous fix (`8f1c9f2`) converted spaces to tabs, which is the wrong direction for this project's `shfmt -i 2` config, causing CI Format + Lint failures. Re-applied correctly: tabs back to 2-space indentation. `shfmt -d` and `shellcheck` both pass (commit `<sha>`).